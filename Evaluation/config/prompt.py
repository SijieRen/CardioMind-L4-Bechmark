diagnosis_list_prompt = """
<预测推理诊断列表>
{}

<标准诊断结果>
{}

<患者病史>（当且仅当执行**下位词匹配**的情况下允许分析病史，使用病史前需明确告知以满足该条件）
{}

## 任务描述
- 根据<标准诊断结果>，运用**核心判定规则和步骤**，判断<预测推理诊断列表>中是否存在与<标准诊断结果>一致的诊断名称，若有，找出1个并返回其在列表中的位置(index)即可；
- **<患者病史>仅可在特定情况下使用**：只有执行到**下位诊断**的判定时，才允许使用和分析<患者病史>进一步确认待预测诊断是否准确，**其他任何情况下禁止回顾、分析<患者病史>**；
- **第一性原则**：在判定过程中，一切以**核心判定规则和步骤**为准，并明确所提供的标准诊断绝对正确，不需要考虑其他任何因素；
- 预测列表中的诊断结果是相互独立的，一旦再判定过程中发现一致的诊断，不需要再考虑列表中的其他诊断（即只需找出1个一致的诊断即可）；同时列表中的多个诊断组合覆盖标准诊断的情况时不允许的。  
- 标准诊断绝对正确，所以只有在遇到下位诊断时才需要考虑病史，当预测诊断时上位词（更宽泛），可直接判定为不一致；

### **核心判定规则和步骤**
- 严格按照以下代码逻辑执行：循环诊断列表，注意列表中多个元素独立执行以下过程，但遇到RETURN时需要立即终止后续所有分析过程并直接给出答案，确保逻辑严谨性以及互不影响。
- 根据代码执行原理：步骤1无需考虑后续步骤2/3/4，同理步骤2无需考虑步骤3/4，依次类推；
- **在对比当前诊断时，不需要处理或考虑预测推理诊断列表 中的其他诊断元素**
- 分析过程请保持逻辑连贯性，尽量避免使用疑问句，避免前后矛盾，严格遵循**第一性原则**要求

```plaintext
FOR each(当前诊断) IN 预测推理诊断列表:
    # --- 步骤1：术语标准化处理 ---
    1.1 IF 当前诊断 与 标准诊断 语义或医学概念直接一致：
          RETURN 存在一致诊断（**立即终止所有循环和思考过程**）  # 强调此处必须立即RETURN
    1.2 对标准诊断和当前诊断进行标准化处理
        1.2.1 术语标准化转换（如AMI→急性心肌梗死）
        1.2.2 去除修饰词/病理（病程）分期的描述：（包括但不限于：可疑、待查、慢性、高危、中度）
    1.3  **将标准化处理后的诊断分别更新到当前诊断和标准诊断 **
    1.4 IF 当前诊断 == 标准诊断：# 注意这里使用标准化后的诊断进行对比，不需要考虑原始名称的影响（例如慢性肺炎与肺炎标注化处理后也属于一致，可直接返回）
            RETURN 存在一致诊断（**立即终止所有循环和思考过程**）  # 强调此处必须立即RETURN

    # --- 步骤2：层级关系判定 ---
    2.1 IF 当前诊断是标准诊断的下位词：
            IF 允许使用病史 AND 病史支持该下位诊断：
                RETURN 存在一致诊断（**立即终止所有分析和循环**） 
            ELSE：
                CONTINUE 下一个诊断循环
    2.2 ELIF 当前诊断是标准诊断的上位词：
            CONTINUE 下一个诊断循环（强制排除上位诊断）

    # --- 步骤3：冲突检测 --- 
    3.1 IF 存在解剖定位/病理类型/时序逻辑冲突：
            CONTINUE 下一个诊断循环

    # --- 步骤4：核心要素匹配 ---
    4. 1 拆分复合诊断：将其拆分为独立的核心要素（如"高血压肾病"→"高血压"和"肾病"）；
    4.2 提取标准诊断核心要素集合S
    4.3 提取当前诊断核心要素集合P
    4.4 IF P完全包含S：
            RETURN 存在一致诊断（**立即终止所有分析和循环**） 
        ELSE：
            CONTINUE 下一个诊断循环

注：
1. CONTINUE表示放弃当前诊断，继续循环中的下一个诊断
2. RETURN表示强制终止逻辑，必须完全终止所有后续分析和循环流程，禁止再分析其他诊断，**避免冗余**；
3. 标准诊断拆分后的多个核心要素需全部被覆盖才满足步骤4
```

### 回复格式（你必须严格按照以下格式生成回复，不要生成其他任务无关内容）
<预测推理诊断列表>中是否存在与<标准诊断结果>一致的诊断：否/是
<预测推理诊断列表>中与<标准诊断结果>一致的诊断(已确认排除所有**上位诊断**的情况)：无/["xx"]
判定为一致的诊断在<预测推理诊断列表>中的位置（从0开始编码）：无/[0]
"""

evidence_hallucination_prompt = \
"""
请根据以下两种情况判断<诊断信息>相对于<病历信息>是否为幻觉信息:
## 情况（1）：如果<诊断信息>中包含例如"缺少...检查"、"未提及...结果"、"无...症状"等否定内容，如果<诊断信息>中所描述的否定内容在<病历信息>中存在，那么<诊断信息>被判断为是幻觉信息，否则不是幻觉信息，判断完成后按照回复格式给出判断结果，不对情况（2）进行判断。
## 情况（2）：如果<诊断信息>中不包含"缺少"等否定内容，直接检测<诊断信息>在<病历信息>中是否存在相关描述，如果不存在相关描述则<诊断信息>被判断为是幻觉信息，否则不是幻觉信息，判断完成后按照回复格式给出判断结果。
<病历信息>
{}

<诊断信息>
{}

【回复格式（按照格式输出判定结果，不需要输出其他内容）】：
是否为幻觉：是/否"""

# 1对多 减少llm调用， 诊断依据 支持/不支持依据 推荐检查 选取依据 溯源使用
trace_query_prompt_o = (
    "## 你的任务是结合<问题>、<上文信息>（若提供）中的内容，根据**用户指定片段**(<待溯源片段>)中的知识点是否有明确、显式地参考<证据集合>中的知识点，判定溯源结果;\n"
    "## 回答要求：\n"
    "- 如果<待溯源片段>是通用，概括性的表述，抽取结果直接输出：[]\n"
    "- 如果<待溯源片段>是基于<上文信息>或<证据集合>的**建议或逻辑推导**，抽取结果直接输出：[]\n"
    "- 如果<待溯源片段>中**未明确**参考<证据集合>中的知识点，抽取结果直接输出：[]\n"
    "- 若当前参考了多个片段，需要都输出出来；\n"
    "- 最终抽取结果以python列表形式返回，例如：['s1','s2']\n"
    "- 若内容未包含参考片段中提供的知识点，抽取结果直接输出：[]\n"
    "## 用户输入\n"
    "### <证据集合>：\n'''{}'''\n\n"
    "### <待溯源片段>：\n'''{}'''\n\n"
)

#跑诊断结果prompt
raw_prompt = """{}
## 任务描述
- 给定患者信息，请按照**临床医学思维**要求，完成临床诊断过程（包括主、次要诊断,鉴别诊断）
- 你需要重点分析患者的所有阴性和阳性特征，包括所有的查体和辅助检查结果，归纳相应的诊断依据，然后动态整合证据链，通过临床推理给出具体的诊断结果；

## **诊断依据要求**：
  - **完整性**：确保将所有与当前诊断相关的依据都列出来，并对**特异性较高的证据加粗显示，单独标注**；
  - **一致性**：所有依据描述尽可能使用原文表述，避免修改、删减、概括，任何情况下禁止捏造事实依据；
  - **顺序性**：严格按**病史、体格检查、辅助检查**对依据进行分类输出，并标明具体类别、按照顺序组织输出，使结果更加清晰；
    - 示例：["病史：xxxx","体格检查：xxxxxxx"]；
    - 仅允许使用上述三个类别进行标注，不要使用其他字段；
  - **鉴别诊断的依据**：鉴别诊断需要列出支持依据和不支持依据，按以下要求归纳到在相应的字段内：
    - **支持依据**：按**病史、体格检查、辅助检查**对依据进行分类，对于关联性证据需合并描述（如体征+影像学特征）；
    - **不支持依据**：
      - 按照“绝对排除证据”和“待验证证据”分类，“待验证证据”需要给出标记，示例：["待验证证据史：xxxx"]；
      - 必须考虑且说明阴性结果的局限性（如检查敏感性不足）； 
    - 未完成的关键检查需标注为“未完成检查”；
    - 若暂无没有不支持依据，允许输出空列表('[]')；

## 临床医学思维
### 主要诊断
- 直接指向患者当前临床表现的根本病因，注意使用符合ICD10的标准疾病名称；
- 病因分型：在主要诊断的基础上，进一步细化疾病的亚型特征；

### 次要诊断
- 一般是指患者的既往史、对患者健康的危害程度相对较小，以及在治疗过程中未占据主要地位的1个或多个疾病；

### 鉴别诊断
- 在初步诊断的基础上，考虑与患者症状和体征相似的其他疾病，并通过进一步的检查和评估来排除这些可能性；
- 要求具备发散性和逆向思维能力，能够从患者临床表现和联想到可能的相关疾病；
- 良好的鉴别诊断有助于提高诊断的准确性，**减少误诊和漏诊的发生**；

### 鉴别诊断选取依据（根据临床思维方法）
  - 选择与本疾病有共同点、部位相邻的疾病相鉴别；
  - 考虑常见病和多发病；
  - 考虑可能导致严重后果的疾病：未及时诊断和治疗可能导致严重后果的疾病，以确保不遗漏危及生命的情况；
  - 必要时考虑地区性、阶段性（流感等）等疾病；
  - 若部分次要诊断会对鉴别诊断产生影响：应判定其可能对并发症状（感染等）、疾病表现和进展、以及鉴别过程的影响；
  - **若已存在明显的不支持证据**，需要直接排除当前待鉴别的诊断，避免无效的鉴别诊断过程和输出；

## 回复格式
- 生成最终的答案时：你必须直接以Json格式（示例如下）输出诊断结果，不要输出任何其他无关内容；
- {"初步诊断": {"主要诊断": {"名称": "XXX", "病因分型": "XXX","诊断依据": ["依据1", "依据2"] }, "次要诊断": [{"名称": "XXX", "诊断依据": ["依据1", "依据2"]}] }, "鉴别诊断": [{"名称": "XXX", "选取依据":"考虑少见但可能致命的疾病", "支持依据": ["依据1", "依据2"],"不支持依据": [], "是否可排除": true, "对应检查推荐": [] }, {"名称": "YYY", "选取依据":"考虑相邻器官病变的鉴别", "支持依据": ["依据1", "依据2"], "不支持依据": ["依据1"], "是否可排除": false, "对应检查推荐": ["建议1", "建议2"] }]}
"""


